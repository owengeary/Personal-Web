/** @type {import('next').NextConfig} */
const nextConfig = {
    // We can keep pageExtensions if we want to support basic MDX pages later, 
    // but for now relying on next-mdx-remote for content.
};

export default nextConfig;
